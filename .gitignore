# Dependencies and local tooling
node_modules/
.playwright-cli/

# Build, test, and debug output
playwright-report/
test-results/
dist-next/
output/
/tmp/
# Redundant with output/ above, but explicit: multi-GB PMTiles basemap extracts
# (scripts/prepare-basemap.js) must never be committed even if output/ narrows.
output/basemap/

# Local environment files
.env
.env.*
!.env.example

# macOS / Finder metadata
.DS_Store
.superpowers/
.geodata-cache/
.playwright-mcp/
